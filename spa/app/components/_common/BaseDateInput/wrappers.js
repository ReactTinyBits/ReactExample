import styled from 'styled-components';
import { DateInput } from 'semantic-ui-calendar-react';

export const DateInputWrapper = styled(DateInput)`
  &&&&&& {
    ui.icon.input {
      width: 100%;
    }
  }
`;
